# Пример конфигурации сервиса-скрапера (localhost / dev).
# В Docker используется projects/scraper/config.yaml с docker-хостами.

database:
  host: localhost
  port: 5432
  user: flight_user
  password: flight_password
  database: flight_prices

rabbitmq:
  host: localhost
  port: 5672
  user: guest
  password: guest
  scrape_queue: scrape_tasks
  job_queue: aviatrade_jobs

redis:
  host: localhost
  port: 6379
  db: 1

prefetch_count: 1
